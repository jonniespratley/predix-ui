import { expect } from 'chai';

import PxTableView from '../../src/px-table-view';

describe('px-table-view', function () {
  it('should do...', function () {
    expect(true).to.equal(true);
  });
});
