import { expect } from 'chai';

import PxActionSheet from '../../src/px-action-sheet';

describe('px-action-sheet', function () {
  it('should do...', function () {
    expect(true).to.equal(true);
  });
});
