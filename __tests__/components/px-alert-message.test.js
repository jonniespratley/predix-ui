import { expect } from 'chai';

import PxAlertMessage from '../../src/px-alert-message';

describe('px-alert-message', function () {
  it('should do...', function () {
    expect(true).to.equal(true);
  });
});
