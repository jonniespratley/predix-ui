import { expect } from 'chai';

import PxAlertLabel from '../../src/px-alert-label';

describe('px-alert-label', function () {
  it('should do...', function () {
    expect(true).to.equal(true);
  });
});
