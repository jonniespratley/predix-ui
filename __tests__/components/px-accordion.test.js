import { expect } from 'chai';

import PxAccordion from '../../src/px-accordion';

describe('px-accordion', function () {
  it('should do...', function () {
    expect(true).to.equal(true);
  });
});
