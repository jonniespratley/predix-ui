import { prop, ifProp, switchProp } from 'styled-tools';
import styled, {
  css,
  createGlobalStyle,
  keyframes,
  ThemeConsumer,
  ThemeContext,
  ThemeProvider,
  ServerStyleSheet,
  StyleSheetManager,
  withTheme,
  isStyledComponent
} from 'styled-components';

export {
  css,
  keyframes,
  createGlobalStyle,
  ThemeProvider,
  ThemeContext,
  ThemeConsumer,
  withTheme,
  ServerStyleSheet,
  StyleSheetManager,
  isStyledComponent,
  switchProp,
  prop,
  ifProp
};

export default styled;
