import { expect } from 'chai';

import {{pascalCase name}} from '../../src/{{dashCase name}}';

describe('{{dashCase name}}', function () {
  it('should do...', function () {
    expect(true).to.equal(true);
  });
});
